name: Hassfest

on:
  push:
  pull_request:

permissions: {}

jobs:
  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Hassfest
        uses: home-assistant/actions/hassfest@master
